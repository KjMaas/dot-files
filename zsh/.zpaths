#!/bin/bash

###################
# my custom paths #
###################

# add local bin folder to path (where nvr resides for exemple)
export PATH="$HOME/.local/bin:$PATH"
export PATH="$HOME/.nvm/versions/node/v17.2.0/bin:$PATH"
